# abfahrplan

This is a simple command-line tool to fetch and display public transportation departure information for a given public transportation stop. It uses the GTFS (General Transit Feed Specification) data format.

See the `Makefile` for an example use-case which fetches departure information for a specific stop in Berlin and generates a compact PDF timetable.
